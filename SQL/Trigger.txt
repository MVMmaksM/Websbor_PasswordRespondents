create trigger Password_UPDATE
ON Password
AFTER UPDATE
AS
UPDATE Password
SET dateupdate= GETDATE()
WHERE id=(select id FROM INSERTED);


USE [Websbor_PasswordRespondents]
GO
/****** Object:  StoredProcedure [dbo].[sp_InsertPassword]    Script Date: 11.02.2022 20:33:42 ******/
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO
ALTER PROCEDURE [dbo].[sp_InsertPassword]
    @ID int out,
	@name nvarchar(100),
    @okpo nvarchar(15),
	@password nvarchar(15),
	@datecreate datetime out,
	@usercreate nvarchar(50) out,
	@dateupdate datetime,
	@userupdate nvarchar(50),
    @comment nvarchar(100)
   
AS
    SET @datecreate=GETDATE()
	SET @usercreate=SUSER_SNAME()

    INSERT INTO Password (name, okpo, password, datecreate, usercreate, dateupdate, userupdate, comment)
    VALUES (@name, @okpo, @password, @datecreate, @usercreate, @dateupdate, @userupdate, @comment)

    SET @ID=SCOPE_IDENTITY()